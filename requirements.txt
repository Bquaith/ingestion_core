# Runtime dependencies for ingestion-platform Python package
# Airflow is intentionally excluded from this environment because
# Apache Airflow keeps SQLAlchemy<2.0 in its runtime, while this package uses SQLAlchemy 2.x.
requests==2.32.5
boto3==1.39.11
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
